# Checked-in defaults, lowest precedence (JVM property > environment variable > this file).
# See docs/architecture.md and docs/payment-safety.md for the full precedence and safety-gate
# rules. Intentionally absent from this file: qa.paymentMode and qa.realCardAuthorizationRef —
# they have no safe default and must be set explicitly by whoever authorizes that run.

qa.baseUrl=https://master--www.ams-qa.finboxgcp.investing.com/pro/
qa.browser=chromium
qa.headless=true
qa.timeoutMs=30000
qa.artifactPolicy=on-failure

qa.allowUserCreation=false
qa.allowSandboxPurchase=false
qa.allowRealCardPurchase=false
