build/
*.class

# Gradle (the wrapper jar itself, gradle/wrapper/gradle-wrapper.jar, IS committed on purpose —
# that's the whole point of the Gradle Wrapper, don't ignore it)
.gradle/

# IDE
# .idea/ and the .iml file are intentionally tracked in this repo (already committed project
# metadata) rather than ignored — don't re-add an ignore rule that contradicts that.
.vscode/

# OS
.DS_Store

# Environment / secrets
.env
.env.*
!.env.example

# Playwright / test artifacts
node_modules/
ms-playwright/

# Logs
*.log
